services:
  - type: web
    name: agni-netra-backend
    env: python
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "cd backend && uvicorn api.server:app --host 0.0.0.0 --port $PORT"
